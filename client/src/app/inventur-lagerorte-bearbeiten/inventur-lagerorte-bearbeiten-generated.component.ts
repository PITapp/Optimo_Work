/*
  This file is automatically generated. Any changes will be overwritten.
  Modify inventur-lagerorte-bearbeiten.component.ts instead.
*/
import { LOCALE_ID, ChangeDetectorRef, ViewChild, AfterViewInit, Injector, OnInit, OnDestroy } from '@angular/core';
import { Router, ActivatedRoute } from '@angular/router';
import { Location } from '@angular/common';
import { HttpClient, HttpParams, HttpHeaders } from '@angular/common/http';
import { Subscription } from 'rxjs';

import { DialogService, DIALOG_PARAMETERS, DialogRef } from '@radzen/angular/dist/dialog';
import { NotificationService } from '@radzen/angular/dist/notification';
import { ContentComponent } from '@radzen/angular/dist/content';
import { TemplateFormComponent } from '@radzen/angular/dist/template-form';
import { LabelComponent } from '@radzen/angular/dist/label';
import { TextBoxComponent } from '@radzen/angular/dist/textbox';
import { RequiredValidatorComponent } from '@radzen/angular/dist/required-validator';
import { DropDownComponent } from '@radzen/angular/dist/dropdown';
import { TextAreaComponent } from '@radzen/angular/dist/textarea';
import { ButtonComponent } from '@radzen/angular/dist/button';

import { ConfigService } from '../config.service';

import { DbOptimoService } from '../db-optimo.service';
import { SecurityService } from '../security.service';

export class InventurLagerorteBearbeitenGenerated implements AfterViewInit, OnInit, OnDestroy {
  // Components
  @ViewChild('content1') content1: ContentComponent;
  @ViewChild('form0') form0: TemplateFormComponent;
  @ViewChild('lagerortNummerLabel') lagerortNummerLabel: LabelComponent;
  @ViewChild('lagerortNummer') lagerortNummer: TextBoxComponent;
  @ViewChild('lagerortNummerRequiredValidator') lagerortNummerRequiredValidator: RequiredValidatorComponent;
  @ViewChild('lagerortTitelLabel') lagerortTitelLabel: LabelComponent;
  @ViewChild('lagerortTitel') lagerortTitel: TextBoxComponent;
  @ViewChild('lagerortTitelRequiredValidator') lagerortTitelRequiredValidator: RequiredValidatorComponent;
  @ViewChild('lagerortStatusLabel') lagerortStatusLabel: LabelComponent;
  @ViewChild('lagerortStatus') lagerortStatus: DropDownComponent;
  @ViewChild('lagerortStatusRequiredValidator') lagerortStatusRequiredValidator: RequiredValidatorComponent;
  @ViewChild('notizLabel') notizLabel: LabelComponent;
  @ViewChild('notiz') notiz: TextAreaComponent;
  @ViewChild('button1') button1: ButtonComponent;
  @ViewChild('button2') button2: ButtonComponent;

  router: Router;

  cd: ChangeDetectorRef;

  route: ActivatedRoute;

  notificationService: NotificationService;

  configService: ConfigService;

  dialogService: DialogService;

  dialogRef: DialogRef;

  httpClient: HttpClient;

  locale: string;

  _location: Location;

  _subscription: Subscription;

  dbOptimo: DbOptimoService;

  security: SecurityService;
  dsoLagerort: any;
  rstLagerorteStatus: any;
  parameters: any;

  constructor(private injector: Injector) {
  }

  ngOnInit() {
    this.notificationService = this.injector.get(NotificationService);

    this.configService = this.injector.get(ConfigService);

    this.dialogService = this.injector.get(DialogService);

    this.dialogRef = this.injector.get(DialogRef, null);

    this.locale = this.injector.get(LOCALE_ID);

    this.router = this.injector.get(Router);

    this.cd = this.injector.get(ChangeDetectorRef);

    this._location = this.injector.get(Location);

    this.route = this.injector.get(ActivatedRoute);

    this.httpClient = this.injector.get(HttpClient);

    this.dbOptimo = this.injector.get(DbOptimoService);
    this.security = this.injector.get(SecurityService);
  }

  ngAfterViewInit() {
    this._subscription = this.route.params.subscribe(parameters => {
      if (this.dialogRef) {
        this.parameters = this.injector.get(DIALOG_PARAMETERS);
      } else {
        this.parameters = parameters;
      }
      this.load();
      this.cd.detectChanges();
    });
  }

  ngOnDestroy() {
    if (this._subscription) {
      this._subscription.unsubscribe();
    }
  }


  load() {
    this.dbOptimo.getInventurBasisByInventurId(null, this.parameters.InventurID)
    .subscribe((result: any) => {
      this.dsoLagerort = result;
    }, (result: any) => {

    });

    this.dbOptimo.getInventurBasisStatuses(null, null, null, null, null, null, null, null)
    .subscribe((result: any) => {
      this.rstLagerorteStatus = result.value;
    }, (result: any) => {

    });
  }

  form0Submit(event: any) {
    this.dbOptimo.updateInventurBasis(null, this.parameters.InventurID, event)
    .subscribe((result: any) => {
      if (this.dialogRef) {
        this.dialogRef.close();
      } else {
        this._location.back();
      }

      this.notificationService.notify({ severity: "success", summary: ``, detail: `Lagerort gespeichert` });
    }, (result: any) => {
      this.notificationService.notify({ severity: "error", summary: ``, detail: `Lagerort konnte nicht gespeichert werden!` });
    });
  }

  button2Click(event: any) {
    if (this.dialogRef) {
      this.dialogRef.close();
    } else {
      this._location.back();
    }
  }
}
